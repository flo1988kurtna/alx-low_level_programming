#include "main.h"

/**
 * _strlen - Returns the length of a string.
 * @str: A pointer to a string.
 * Return: The length of the string.
 */
size_t _strlen(const *str)

{
	size_t length = 0;

	while (*s != '\0')
	{
		length++;
		s++;
	}

	return (length);
}

